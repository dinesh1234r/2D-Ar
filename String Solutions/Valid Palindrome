class Solution {
    public boolean isPalindrome(String s) {
        s=s.toLowerCase();
        s=s.replaceAll("[^a-z0-9]","");
        StringBuilder str=new StringBuilder();
        str.append(s);
        str.reverse();
        if(s.contains(str))
        {
            return true;
        }
        return false;
    }
}
